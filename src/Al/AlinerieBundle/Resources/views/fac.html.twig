<div class="col-sm-12 col-md-4">
    
     {% for fac in unFac %}
			<h3><span class="bold">QUESTION|</span>réponse.</h3><i>Parce que vos questions comptent ...</i>
			<div class="QR"><br/>
				<span style="text-align:center;"><i class="fa fa-quote-left"></i>  {{ fac.title }}<br/><strong>{{ fac.author }}</strong></span><p><br/>{{ fac.content }}
				
			</div>
    
    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
     <p>
    <a href="{{ path('al_alinerie_editaside', {'aside_id': fac.id}) }}" class="btn btn-default">
      <i class="fa fa-pencil fa-1x"></i>
      Modifier l'encart
                </a></p>
    {% endif %}
    
     {% endfor %}
		</div>